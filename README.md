# layered-representation-of-images
Given a single bitmap RGB image, generate a layered representation that separates the scene into interpretable, re-composable layers (e.g., foreground objects, background “stuff”, depth-ordered planes, and optional appearance layers like albedo/shading). which are useful for downstream vision tasks
